# DB 백업/복구 리허설 · 롤백 절차 — 2026-09-01 정식 운영 전환용

## 개요
- 이 문서는 두 가지를 다룸: ① DB 데이터가 잘못됐을 때 되돌리는 방법, ② 배포한 코드에 문제 있을 때 되돌리는 방법
- 지금까지는 `scripts/prod_cleanup_2026-08-01.sql`처럼 "실행 전 Supabase Dashboard에서 백업 확보" 정도로만 관례가 있었고, 정식 절차는 없었음 → 이 문서로 정리함
- 정식 운영 전환 전에 실제로 한 번은 리허설(연습) 해보는 걸 목표로 함

---

## 1. DB 백업 확보 방법

- 기본 수단: **Supabase Dashboard > Database > Backups**
- [ ] 현재 Supabase 요금제에서 백업이 자동(매일)으로 도는지, 수동으로 눌러야 하는지 확인 필요함
- [ ] Point-in-Time Recovery(특정 시각으로 복원) 지원 여부도 요금제별로 다르므로 확인 필요함 — 지원 안 되면 "특정 시점 백업본"으로만 복원 가능
- 위험한 작업(대량 삭제/수정 SQL, 마이그레이션 등) 실행 전에는 아래 순서를 지킴
  1. Supabase Dashboard에서 수동 백업(또는 스냅샷) 생성함
  2. 백업 생성 완료 확인함 (진행 중 상태에서 바로 작업 시작하지 않기)
  3. 작업 실행함
  4. 작업 직후 카운트 쿼리 등으로 결과 검증함 (`prod_cleanup_2026-08-01.sql`처럼 COMMIT 전에 확인 쿼리 넣는 방식 유지)

---

## 2. DB 복구(복원) 절차

- Supabase Dashboard에서 원하는 백업 시점 선택 후 복원 실행함
- [ ] 실제로 복원 버튼을 눌러본 적이 없다면, 배포 전 리허설 때 **테스트용 프로젝트**나 **로컬 Postgres**로 한 번 시도해볼 것을 권장함 (운영 DB에 바로 연습하지 않기)
- 복원 시 주의할 점
  - 복원 시점 이후에 쌓인 데이터(신규 응시 기록, 신규 회원 등)는 전부 사라짐 → 복원은 "최후의 수단"으로만 사용함
  - 복원 중에는 서비스가 불안정할 수 있으므로, 가능하면 점검 모드(`/admin/maintenance`)를 먼저 켜고 진행함
- 복구 후 확인 절차
  1. 주요 테이블 row 수 확인 (department, subject, exam, exam_question, users)
  2. 관리자 로그인 정상 동작 확인
  3. 시험 목록/응시 화면 정상 동작 확인
  4. 점검 모드 해제

---

## 3. Flyway 마이그레이션 롤백 원칙

- 현재 사용 중인 Flyway는 **자동으로 마이그레이션을 되돌리는 기능(Undo)이 없는 버전/구성**임
- 따라서 "V8을 취소한다" 같은 개념이 없고, 문제가 생기면 **새 마이그레이션 파일을 추가해서 앞으로 나아가며 고치는 방식(forward-fix)**을 원칙으로 함
  - 예: V8이 잘못됐으면 V8을 지우지 말고 V9에서 수정함
- 이미 운영 DB에 적용된 마이그레이션 파일(V1~V8)은 **절대 수정하지 않기** — Flyway가 체크섬으로 변경을 감지해서 배포 자체가 실패할 수 있음
- 데이터까지 되돌려야 하는 심각한 경우에만 위 1~2번(백업 복원)을 사용함

---

## 4. 애플리케이션(Render) 배포 롤백 절차

- [ ] Render 대시보드에서 "이전 배포로 되돌리기(Rollback/Redeploy previous)" 기능 위치 미리 확인해두기
- 절차
  1. Render 대시보드 > 서비스 > Deploys 탭 이동함
  2. 문제 생기기 직전, 정상 동작했던 배포(커밋) 확인함
  3. 해당 배포를 다시 배포(Redeploy)함
  4. 롤백 완료 후 스모크 테스트 체크리스트 중 핵심 항목(로그인, 시험 응시, 관리자 화면)만이라도 재확인함
- 주의: 코드만 롤백하고 DB 마이그레이션은 이미 적용된 상태로 남아있을 수 있음 → 코드 롤백과 DB 상태가 서로 맞는지(예: 새 컬럼을 쓰는 코드가 없어졌는데 컬럼은 남아있는 경우) 확인 필요함. 컬럼이 남아있는 것 자체는 대부분 문제 없음(이전 코드가 안 쓰는 컬럼일 뿐)

---

## 5. 배포 전 리허설 체크리스트

- [ ] 로컬 또는 테스트 환경에서 실제 DB 백업 1회 수행해봄
- [ ] 백업본으로 복원 1회 수행해봄 (운영 DB 아닌 곳에서)
- [ ] Render에서 "이전 배포로 되돌리기" 버튼 위치와 소요 시간(체감) 확인해봄
- [ ] 이 문서에 있는 절차 중 실제 화면과 다른 부분 있으면 스크린샷/메뉴 경로로 업데이트함
