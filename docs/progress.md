# 세션 진행 상황 트래커

> 여러 Claude Code 세션(웹/로컬)이 병렬로 작업할 때 서로 컨텍스트를 공유하기 위한 문서입니다.
> 각 세션은 메모리를 공유하지 않으므로, 설계안·진행 상황·다음 할 일을 여기에 기록해두면
> 다른 세션(다른 브랜치, 로컬 터미널 포함)에서 이어서 작업할 수 있습니다.

## 사용법

1. 작업을 시작하거나 끝낼 때 아래 표에 자신의 브랜치 행을 추가/갱신하세요.
2. 설계안처럼 내용이 긴 것은 이 파일에 직접 쓰지 말고 `docs/design/<주제>.md` 파일로 따로 만든 뒤,
   이 표의 "설계 문서" 칸에 경로를 링크하세요.
3. 이 파일은 **main 브랜치에 직접** 커밋합니다 (기능 브랜치에 두면 다른 세션이 못 봄).
   - `git checkout main && git pull origin main`
   - 표 수정 후 `git add docs/progress.md && git commit -m "docs: progress 업데이트" && git push origin main`
   - 작업하던 브랜치로 다시 돌아가는 것 잊지 마세요: `git checkout <내 브랜치>`
4. 로컬 터미널에서 이어서 작업할 때는 `git fetch origin main && git show origin/main:docs/progress.md`
   또는 `git pull origin main` 후 이 파일을 열어서 현재 상황을 파악하세요.

## 세션 목록

| 브랜치 | 목적 | 상태 | 설계 문서 | 다음 할 일 | 마지막 업데이트 |
|---|---|---|---|---|---|
| `claude/memory-cli-workflow-8847cj` | 세션 간 작업 공유 워크플로우 정리 (이 파일 자체) | 완료 | - | - | 2026-08-06 |
| `claude/beta-notice-message-update-xglihe` | 메인화면 Beta 안내 문구에 오탈자/버그 양해 문구 추가 및 줄바꿈 정리 (`index.html`) | 완료 | - | - | 2026-08-06 |
| `claude/notice-detail-top-buttons-v0klmh` | 공지사항 상세 화면(`noticeDetail.html`)에 하단에만 있던 수정/삭제/목록 버튼을 상단에도 동일하게 추가 | 완료 | - | - | 2026-08-06 |
| `claude/notice-post-guidelines-iebqwe` | 실제 공지사항 게시판에 올릴 글감 기획 및 초안 작성 — 서비스 소개, 이용 매뉴얼, 버그·오타 제보(+면책 문구), 기출자료 제공 요청 4종 + 향후 반복 게시할 기출/기능 업데이트 소식용 제목·본문 템플릿. 코드 변경 없이 대화로 콘텐츠만 작성(웹 에디터에 직접 붙여넣는 방식). **사용자가 이후 실제 공지사항 웹 에디터에 4개 초안 게시까지 완료함** | 완료 (게시까지 완료) | - | - | 2026-08-18 |
| `claude/deployment-user-screen-nl15mb` | 정기/긴급 배포 시 점검 모드 및 사전 공지 배너 기능 추가. DB 기반 점검 플래그를 관리자 페이지(`/admin/maintenance`)에서 즉시 토글, 인터셉터가 점검 중 요청을 안내 화면(503)으로 전환하되 ROLE_ADMIN 세션과 이미 응시 중이던 시험의 최종 제출(`POST /exams/{id}/solve`)은 통과시킴. 정기점검 사전 공지용 배너를 메인 레이아웃 상단에 별도 노출. 배포 후 실서비스에서 설정 저장 시 500 에러 발생 — `SiteOperationSetting`에 Lombok getter(`getMaintenanceEnabled`)와 직접 만든 `isMaintenanceEnabled()`가 같은 프로퍼티에 공존해 MyBatis가 getter를 특정 못 하던 버그였고, 헬퍼 메서드 제거 + 회귀 테스트 추가로 수정. 이후 점검 화면이 부트스트랩 기본 스타일이라 브랜드가 안 느껴진다는 피드백을 받아, 메인 화면과 동일한 인디고 포인트 컬러/카드 톤/잉크 네비·풋터로 리디자인(브랜드 로고, 점검중 배지, 예상 종료 시각 배지, 60초 자동 새로고침 포함). 모두 로컬 Postgres+헤드리스 크롬 스크린샷으로 검증 후 main에 병합·배포 완료 | 완료 | - | - | 2026-08-06 |
| `claude/remove-roadmap-md-b1ytt3` | `ROADMAP.md` 삭제 및 `CLAUDE.md`의 참조 문구 정리 | 완료 | - | - | 2026-08-06 |
| `main` (로컬) | 관리자 UI 개선 5건: (1) 문제 없는 시험 "문제 풀기" 클릭 시 alert 대신 커스텀 토스트(`appToast`, `fragments/dialogs.html`+`ui-dialog.js`+`app.css`) 안내, (2) 엑셀 문제 업로드 시 파일 내 시험ID 불일치·현재 화면 시험ID와 불일치·문제번호 누락이면 `AdminExamQuestionViewController`에서 차단, (3) `solve.html`/`preview.html` 보기 원형 숫자(①②③④) 크기 확대 및 텍스트·이미지 보기 스타일 통일, (4) 관리자 시험 목록(`exam/examList.html`, "시험 보기" 화면)에 미사용 시험만 보기 체크박스 추가(서버는 이미 `useYn` 파라미터 지원 중이었음), (5) 과목/학과/시험관리 폼의 사용여부 select를 라디오 버튼으로 변경. 로컬 Docker Postgres + `bootRun`으로 관리자 세션 로그인 후 curl로 각 화면 렌더링·엑셀 업로드 3종 시나리오 직접 검증 완료(Claude-in-Chrome 확장 미연결로 스크린샷 대신 서버 응답 HTML로 확인) | 완료 | - | - | 2026-08-07 |
| `claude/pre-deployment-documentation-3me0m2` | 9/1 정식 배포 전 개발문서(내부)·기술블로그(공개) 소재 정리 및 초안 작성. `docs/writing-topics.md`를 Tier1(배포 전 필수)/Tier2(설계·트러블슈팅 로그, 내부용)/Tier3(공개 블로그)로 우선순위 재정리. 코드·커밋 기록(마이그레이션 파일, `GlobalExceptionHandler`, `LoginAttemptService`, `SiteOperationSetting` 등)을 실제로 조사해 근거 있는 초안을 `docs/drafts/`에 12건 작성: Tier1 7건(배포 전 체크리스트, 스모크 테스트, Sentry 모니터링, 관리자 계정 매뉴얼, DB 백업/복구, Render GitHub App 재연결, 점검모드 매뉴얼) 전부, Tier2 5건(YAML multipart 트러블슈팅, 로컬 개발환경 가이드, 예외처리 컨벤션, use_yn 계층 설계, DB 마이그레이션 의사결정 로그) 완료. 조사 중 `application-prod.yml`에 sentry 설정 섹션이 누락된 것을 발견해 체크리스트에 반영함. **(2026-08-18 추가) 사용자가 Tier2 나머지 5건은 작성하지 않기로 결정함 — 더 이상 할 일 목록에 포함하지 않음** | 완료 (Tier1 전체 + Tier2 일부, 나머지 Tier2는 진행 안 하기로 결정) | - | Tier3(블로그 23건)는 착수 전 — 필요 시 `docs/writing-topics.md`의 "추천 착수 순서" 1~5번부터 이어서 쓰면 됨. 초안이 실제 게시판/블로그에 옮겨지면 `docs/drafts/`는 삭제 가능 | 2026-08-18 |

| `claude/readme-remove-broken-images-xwze64` | `README.md`의 "📸 화면 예시" 섹션 제거 — Kakao/Tistory CDN 이미지 링크(`expires` 파라미터 만료)가 전부 엑박으로 뜨는 문제 해결. 이미지 없이는 항목 텍스트만 남아 의미가 없어 해당 섹션 전체(사용자/관리자 화면 캡처 목록) 삭제 | 완료 | - | - | 2026-08-11 |
| `main` (로컬) | 5주차 항목 중 비용 없는 두 개(롤백/DB 백업 복구 리허설)를 미리 진행. **DB 백업 복구 리허설 완료**: 로컬에 있던 예전 백업 파일(`db_cluster-21-09-2025@15-12-39.backup`, 2026-05-12 Phase 0 시점)을 로컬 Docker Postgres 새 컨테이너에 복원 → Flyway가 V2~V8 마이그레이션을 forward-fix 방식으로 전부 정상 적용 → 앱 기동 + `/health` 정상까지 확인, 절차 자체는 검증됨. **진행 중 발견한 중요 사실**: Supabase 대시보드에서 확인해보니 **무료 플랜은 프로젝트 백업이 아예 없음**("Free Plan does not include project backups", Pro 업그레이드해야 최대 7일 백업 제공) — 즉 5주차에 Pro로 업그레이드하기 전까지는 운영 DB에 실제 자동 백업이 존재하지 않는 공백 기간임. 사용자가 "5주차까지 약 1주일 정도라 리스크 감수하고 넘어가기"로 결정함(수동 pg_dump 백업 등 대안도 제시했으나 기각). Render 롤백 리허설은 브라우저 확장 미연결로 사용자가 직접 대시보드에서 진행하려 했으나, 절차 자체가 이미 `docs/drafts/tier1-05-db-backup-restore.md`에 문서화돼 있고 Render의 표준 원클릭 기능이라 낮은 리스크로 보고 **사용자 판단으로 보류(패스)함** — 5주차나 오픈 직전에 필요하면 이어서 진행. 마지막으로 `README.md`에 실제 서비스 링크(`www.knou-cbt.com`) 추가 후 이번 주 작업 마무리 | 완료 (이번 세션 작업 전체) | - | 5주차(8/25~8/28) 되면 시작: Render Starter+Supabase Pro 업그레이드(백업 공백을 없애는 의미도 있어 우선순위 높게 볼 것), 여유 있으면 Render 롤백 리허설. 애드센스는 심사 결과 대기 중 | 2026-08-17 |
| `claude/custom-domain-update` (main에 병합·배포 완료) | 4주차 로드맵 진행: (1) 가비아에서 `knou-cbt.com` 구매 + Render 커스텀 도메인 연결(A 레코드 `@`/`www` → `216.24.57.1`) 완료, `robots.txt`/`sitemap.xml`의 도메인 교체 완료. 진행 중 사용자가 KNOU 상표/기출문제 저작권 리스크를 질문해 조사함 — 도메인 자체보다 **기출문제 원문 재배포의 저작권이 핵심 리스크**(대법원 판례상 시험문제는 창작성 인정 저작물, "영리 목적 재배포는 면책 안 됨"), 다만 동일 컨셉의 개인 사이트(`allaclass.tistory.com`, 방통대 기출+애드센스)가 오래 운영 중이라 실질 단속 리스크는 낮다고 보고 사용자가 진행 결정함. GA 새 도메인 스트림 등록은 사용자가 직접 해야 함(미완료). (2) **Sentry 알림 이메일 테스트 — 완료, 실제 버그 발견/수정함**: `application-prod.yml`에 빠져있던 `sentry:` 블록 추가 + 검증용 `/admin/debug/test-error`(ROLE_ADMIN 전용) 엔드포인트 추가 후에도 알림이 전혀 안 와서 `sentry.debug=true`로 원인 추적 → **`sentry-spring-boot-starter-jakarta`가 `sentry-logback`을 transitive로 안 끌고 오고, 커스텀 `logback-spring.xml`을 쓰고 있어서 Sentry의 로그 자동 캡처 연동 자체가 걸려있지 않았던 게 근본 원인**(Sentry 전송 자체는 정상이었음 — SDK가 자체적으로 보내는 성능추적 트랜잭션은 잘 갔는데 `log.error()` 호출만 전혀 이벤트화 안 됨). `sentry-logback` 의존성 추가 + `logback-spring.xml`에 ERROR 필터 걸린 `SentryAppender`를 root logger에 명시적으로 연결해서 해결, 실제 테스트로 Sentry Issues 캡처 + 알림 이메일 수신까지 확인 완료. 이 과정에서 부수적으로 발견한 버그도 같이 수정: 봇이 존재하지 않는 정적 파일(`js/twint_ch.js` 등)을 스캔할 때 발생하는 `NoResourceFoundException`이 catch-all 핸들러로 빠져 ERROR로 Sentry에 계속 쌓이던 걸 기존 404 핸들러 그룹에 합류시켜 조용히 처리되도록 수정. 테스트용 엔드포인트는 사용자 결정으로 그대로 운영 점검용으로 남겨둠. 관리자 비밀번호를 사용자가 순간 분실했다가 직접 찾아서 DB 리셋은 진행 안 함. (3) **애드센스 신청 — 사이트 추가·인증까지 완료, 심사는 백그라운드 대기 중**: 기존에 티스토리 블로그와 연결된 애드센스 계정을 그대로 써서 `knou-cbt.com`을 사이트로 추가함(새 계정 생성은 피함 — 구글 정책상 동일인 중복 계정은 실명/주소/결제정보/기기지문 등으로 탐지되어 기존 계정까지 정지될 위험이 있고, 특히 저작권처럼 "심각한 위반" 카테고리는 사이트 단위가 아니라 계정 단위 정지로 번질 수 있어 신중히 진행). 소유권 인증은 `ads.txt` 방식 선택(광고 스크립트 방식은 CSP `script-src`를 같이 열어줘야 해서 승인 전엔 보류, 나중에 실제 광고 켤 때 CSP와 같이 처리 예정). **진행 중 실제 버그 발견**: `robots.txt`/`sitemap.xml`이 SecurityConfig `permitAll` 목록에 없어서 비로그인 요청이 `/admin/login`으로 리다이렉트되고 있었음(즉 지금까지 검색엔진 크롤러가 이 파일들을 정상적으로 못 읽었을 가능성이 높음, `ads.txt`도 이대로면 구글 크롤러가 인증 못 함) → 세 파일 모두 `permitAll`에 추가해서 해결. 동의 메시지(EU GDPR 대응)는 Google CMP의 "3가지 선택사항(동의/동의하지 않음/옵션 관리)" 방식으로 선택 — 2가지 선택사항(동의/옵션관리만)은 거부가 원클릭이 아니라 최근 규제기관들이 비적합 판단하는 패턴이라 제외함. (4) **GA 새 도메인 스트림 등록 — 완료**: 기존 스트림 URL을 `knou-cbt.com`으로 수정(측정 ID `G-2J0NPPN1MD` 그대로라 코드 변경 없음), 실시간 보고서로 수집 확인함. **이로써 로드맵 4주차 전 항목 완료**, 원래 5주차 항목이던 "robots.txt/sitemap.xml/GA를 새 도메인으로 교체"도 이번에 같이 끝나서 5주차에서 제외 가능. (5) 이 진행 상황을 정리한 아티팩트(주차별 카드+콜아웃 형태 로드맵)를 만들어둠 → https://claude.ai/code/artifact/bb0cbbfb-d580-414f-aa3f-601b41171a44 | 완료 (4주차 전체) | - | 로드맵 5주차(8/25~8/28): Render Starter+Supabase Pro 유료 업그레이드, 롤백 리허설, DB 백업 복구 리허설. 애드센스는 심사 결과 대기 중 — 승인되면 광고 스크립트 삽입 + CSP script-src에 구글 광고 도메인 추가 필요 | 2026-08-17 |
| `main` (로컬) | Sentry가 실제로 잡아준 운영 에러 첫 건 대응: `page=0`으로 시험 목록 조회 시 `Validation failed ... must be greater than or equal to 1` 500 에러. 원인은 `ExamViewController`(및 동일 패턴의 Subject/Department/Notice 목록 컨트롤러)가 `@Valid PageRequest`를 쓰고 있어서, `PageRequest`에 이미 있던 `pageOrDefault()`/`sizeOrDefault()` 방어 로직이 실행되기도 전에 검증 실패로 500이 나던 것 — 방어 코드는 있는데 `@Valid`가 먼저 막아버려서 무용지물이었음. 게다가 `examList.html`의 "이전" 버튼이 1페이지에서도 `href`에 `page=0`을 그대로 넣고 있어서(시각적으로만 disabled) 앱 스스로도 이 에러를 유발할 수 있는 구조였음. 4개 컨트롤러 전부에서 `@Valid` 제거하고 `sizeOrDefault()`에 `Math.min(size, 100)` 상한을 추가해 `@Max(100)` 제거로 없어지는 보호도 유지함. 로컬에서 `page=0`/`page=-1`/`size=99999` 전부 200으로 정상 처리되는 것까지 curl로 확인 후 배포 | 완료 | - | 사용자가 원하면 이것도 Tier2 트러블슈팅 로그로 문서화 가능(제안만 해둠, 작성은 안 함) | 2026-08-17 |
| `claude/knou-cbt-memory-limit-11upgq` | Render가 "Web Service knou-cbt exceeded its memory limit" 알림 메일을 보내와서 원인 조사 요청 받음(코드 변경 없이 기록만 남기는 세션). Render Free 인스턴스가 512MB 한도를 넘겨 자동 재시작됨. `Dockerfile`에 JVM 힙 상한(`-Xmx`/`-XX:MaxRAMPercentage` 등)이 전혀 지정돼 있지 않아 원래도 512MB에서 여유가 빠듯했던 상태였고, 공교롭게도 직전 세션(`claude/custom-domain-update`)에서 `robots.txt`/`sitemap.xml`의 로그인 리다이렉트 버그를 막 고쳐 검색엔진 크롤러가 처음으로 정상 색인을 시작한 시점이라 크롤링 트래픽 유입이 유력한 원인으로 추정됨(Render 대시보드 로그·메트릭 직접 접근 권한이 없어 그래프로 확정하지는 못함). 사용자가 "5주차(8/25~8/28)에 이미 계획된 Render Starter 업그레이드로 자연스럽게 해결될 문제"로 판단해 코드 수정은 보류하고 기록만 남기기로 결정함 — 로드맵 아티팩트(`https://claude.ai/code/artifact/bb0cbbfb-d580-414f-aa3f-601b41171a44`)에도 동일 내용의 알림 카드 추가함 | 완료 (기록만) | - | 5주차 Render Starter 업그레이드 시 이 이슈가 재발하지 않는지 확인. 업그레이드 후에도 재발하면 그때 `-XX:MaxRAMPercentage` 등 JVM 힙 튜닝 검토 | 2026-08-18 |
| `claude/weekly-tasks-c9wtvg` | 5주차(8/25~8/28) 로드맵 항목 중 **Render Starter + Supabase Pro 유료 업그레이드 완료**(코드 변경 없음, 대시보드 결제 전환 작업 — 사용자가 직접 진행, 세션은 절차 안내만 담당). Render는 서비스 페이지 → Scaling 탭 → "Enable Scaling" 안내에서 나오는 "Review upgrade options" 경로로 Instance Type을 Free→Starter로 전환(워크스페이스 전체 Plan인 Hobby/Pro/Scale과는 별개 설정이라 헷갈리기 쉬움 — 워크스페이스 Plan은 Hobby 그대로 유지, 서비스별 Instance Type만 유료 전환하면 됨). Supabase는 Project Settings → Billing에서 Pro 전환. 사용자가 두 전환 모두 완료했다고 확인함. 세션 샌드박스가 아웃바운드 네트워크 화이트리스트로 제한돼 있어(`curl`/`WebFetch` 모두 `knou-cbt.com`에 egress 차단) 라이브 사이트 정상 동작은 세션에서 직접 검증하지 못함 — 사용자가 직접 확인 필요. **로드맵 아티팩트도 업데이트함** → https://claude.ai/code/artifact/bb0cbbfb-d580-414f-aa3f-601b41171a44 | 완료 (업그레이드 전환) | - | 이전 세션(`claude/knou-cbt-memory-limit-11upgq`)이 남긴 후속 확인 항목: Render Starter 전환 후 메모리 초과 재발 여부 관찰(재발 시 `-XX:MaxRAMPercentage` 등 JVM 힙 튜닝 검토). Supabase Pro의 Database → Backups(PITR/Daily Backup) 실제 활성화 여부는 대시보드에서 별도 확인 필요. 여유 있으면 Render 롤백 리허설(절차는 `docs/drafts/tier1-05-db-backup-restore.md`에 문서화돼 있음) | 2026-08-24 |
| `claude/google-adsense-approval-docs-95p157` (main에 병합·배포 완료) | **구글 애드센스 심사 승인됨** (8/17 `claude/custom-domain-update`에서 `ads.txt`로 소유권 인증 후 신청, 심사 대기 상태였던 건). (1) 승인 사실을 이 표 + 로드맵 아티팩트(`https://claude.ai/code/artifact/bb0cbbfb-d580-414f-aa3f-601b41171a44`)에 기록. (2) **광고 게재 구현**: `layout.html` head에 애드센스 Auto ads 스크립트(`pub-2360128741368426`) 삽입, `SecurityConfig`의 CSP에 광고 렌더링에 필요한 구글 도메인(`pagead2.googlesyndication.com`, `googleads.g.doubleclick.net`, `tpc.googlesyndication.com` 등)을 `script-src`/`img-src`/`connect-src`/`frame-src`에 추가. 로컬 Postgres(`pg_ctlcluster`) + `bootRun` + 헤드리스 크롬으로 홈 화면이 정상 렌더링되고 CSP 헤더에 새 도메인이 반영됨을 확인, 브라우저 콘솔에도 CSP 위반(Refused to load 등) 로그 없음을 확인함(단, 샌드박스 자체의 아웃바운드 네트워크 제약으로 외부 도메인 리소스 로드 자체는 실패 — CSP와 무관한 별개 이슈이며 실제 배포 환경에서는 발생하지 않음). (3) **main에 병합·푸시 완료**(커밋 `ce0ee0f`) — 이 세션의 샌드박스 자체는 아웃바운드 네트워크가 제한돼 있어 직접 확인은 못했으나, **사용자가 실제 `www.knou-cbt.com` 응답 헤더를 캡처해 전달** — `content-security-policy` 헤더에 `pagead2.googlesyndication.com`/`googleads.g.doubleclick.net`/`tpc.googlesyndication.com`이 `script-src`/`img-src`/`connect-src`/`frame-src`에 정확히 반영돼 있고 `server: cloudflare` + `x-render-origin-server: Render`로 실제 운영 서버 응답임을 확인 → **배포 라이브 반영 검증 완료**, 사용자가 Network 탭에서 `adsbygoogle.js` 200 OK 로드까지 확인. (4) **후속 CSP 버그 발견·수정**: 라이브 반영 후 사용자가 브라우저 콘솔에서 `Refused to connect to 'https://ep1.adtrafficquality.google/...' because it violates ... connect-src` 에러를 발견해 전달함 — 구글 광고 트래픽 품질(어뷰징/봇) 검증 서비스 도메인이 CSP에 빠져 있던 것. `SecurityConfig`의 `script-src`/`connect-src`/`frame-src`에 `*.adtrafficquality.google` 추가해 수정, 로컬 `bootRun`으로 CSP 헤더 반영 확인 후 main에 병합·푸시(커밋 `9c22fe4`). (5) **재배포 후 같은 도메인의 `img-src` 차단을 사용자가 또 발견** — sodar 픽셀(`https://ep1.adtrafficquality.google/pagead/sodar?...`)이 `<img>` 요청으로도 로드되는데 `img-src`에는 `adtrafficquality.google`을 빠뜨렸던 것. `img-src`에도 `*.adtrafficquality.google` 추가해 수정, 로컬 CSP 헤더 확인 후 main에 병합·푸시(커밋 `6078a18`). (6) **재배포 후 재확인 — CSP 문제는 완전히 해소, 남은 403은 CSP와 무관한 정상 현상으로 판단**: 사용자가 다시 콘솔을 확인했고 `googleads.g.doubleclick.net/pagead/ads?...` 요청에서 403이 뜨는 게 남아있었는데, 이 에러에는 "violates Content Security Policy" 문구가 없어 CSP 차단이 아니라 **구글 광고 서버 자체가 응답한 403**임을 확인함 — 요청 파라미터의 `adx=-12245933&ady=-12245933`는 구글이 슬롯 위치 확정 전 예비 확인 요청에 붙이는 특수값이라 실제 광고 미노출 상태의 정상 신호로 판단, 승인 직후 웜업 기간·반복 새로고침에 의한 무효 트래픽 방지 로직 등이 원인일 수 있음 — **코드로 대응할 사항 아님, 액션 아이템 없음** | 완료 (CSP 이슈 전부 해소, 애드센스 코드/배포 작업 최종 마무리) | - | 애드센스 대시보드에서 광고 노출/수익 발생 확인 — 1~2주 지나도 계속 403만 뜨고 노출 수가 0이면 그때 재점검. 그 외 별도 작업 불필요 | 2026-08-18 |
| `claude/knou-cbt-connection-pool-check-lh5qii` (main에 병합·푸시 완료, PR 없이 직접 병합) | **9/1 오픈 대비 DB 커넥션 풀 고갈 예방 점검** — 같은 Supabase 조직의 다른 프로젝트(gelly)가 실사용자 100명/2시간 만에 "백엔드가 자기 커넥션 풀을 무제한으로 열어 Nano 티어 풀(15개) 대부분을 혼자 점유 → 로그인/조회 전체 막힘" 장애를 겪어서, 같은 패턴이 knou-cbt에도 있는지 확인 요청받음. **코드 조사 결과**: knou-cbt는 Prisma 아니고 Spring Boot+MyBatis+HikariCP(JDBC) 조합이며, `application.yml`에 이미 `hikari.maximum-pool-size: 5`, `minimum-idle: 0`이 명시돼 있어 gelly처럼 무제한으로 열리는 구조가 아니었음(좋은 소식). 사용자가 Supabase 대시보드에서 직접 확인해준 값: Compute **Nano**, Connection pool size **15**, `SPRING_DATASOURCE_URL`이 포트 **6543**(Transaction 모드, Supavisor pooler)이고 `?prepareThreshold=0`까지 이미 붙어있어 JDBC 드라이버 prepared statement 이슈까지 미리 대응돼 있었음 → gelly의 위험 조합(Session 모드+풀 무제한)과 다르다고 판단. 다만 max=5는 Nano 풀(15) 대비 다소 빡빡해서 동시접속 스파이크 시 6번째 요청부터 Hikari 기본 `connectionTimeout`(30초) 대기가 걸릴 수 있음. 사용자와 상의 후 3가지 안전장치를 코드로 반영: (1) `hikari.maximum-pool-size`를 `${SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE:8}`로 바꿔 기본값 8로 상향하면서 배포 없이 Render 환경변수만으로 즉시 조정 가능하게 함, (2) `exam-list.js`/`subject-list.js`(부서→과목/학년→과목구분 등 캐스케이딩 드롭다운)와 `notice-form.js`(공지 이미지 업로드)의 `fetch()` 호출에 타임아웃/에러 처리가 전혀 없어 서버가 응답 못 주면(예: DB 풀 대기) 무기한 대기하던 문제를, `ui-dialog.js`에 추가한 공용 `fetchWithTimeout()`(기본 8초, 이미지 업로드는 15초) + `handleFetchError()`(토스트 안내)로 해결, (3) `question-manage.js`가 문제 저장/엑셀 업로드 시 띄우는 전체화면 로딩 오버레이(`showLoading`)가 서버 응답 지연 시 무한 로딩처럼 보이던 것을, `ui-dialog.js`의 `showLoading()`에 15초 후 안내 문구로 자동 교체되는 타이머를 추가해 개선(네이티브 form submit이라 진짜 취소는 불가하지만 최소한 "지연되고 있다"는 안내는 나가도록). **프론트 자동 로그아웃 버그(gelly에서 같이 발견된 패턴)는 knou-cbt에 없음을 확인** — 인증이 JS가 아니라 서버사이드 Spring Security 세션 쿠키 방식이고, `GlobalExceptionHandler`의 catch-all(`Exception.class`)도 500 페이지만 반환할 뿐 세션/인증 상태는 건드리지 않음. 로컬 Postgres(`pg_ctlcluster`)+`bootRun`으로 새 Hikari 설정 기동 확인(9개 Flyway 마이그레이션 정상 적용) 및 홈/시험목록/정적 JS/`/api/departments/{id}/subjects` curl로 200 확인, 수정한 JS 4개 파일 전부 `node --check` 구문 검증 통과, `./gradlew compileJava` 정상. **병합 전 Supabase 상태 인시던트("Error spikes in one of the ap-northeast-2 Supavisor clusters", ECHECKOUTTIMEOUT) 발생 중이라 사용자가 일시적으로 main 푸시 보류 요청** → knou-cbt의 `SPRING_DATASOURCE_URL`은 `ap-southeast-1` 리전 pooler라 해당 인시던트(`ap-northeast-2`, 서울)와 다른 클러스터임을 확인 → 영향 가능성 낮다고 판단해 사용자 승인 하에 **main에 직접 병합·푸시 완료**(병합 커밋 `3181b9e`, PR 없이 진행). 병합 과정에서 서비스 DB 리전이 애초에 왜 싱가포르(ap-southeast-1)로 선택됐는지도 논의됨 — 코드/커밋 히스토리에 근거가 없어 "서울 리전이 나중에 추가됐을 가능성" 등 추정만 제시, 결론 없이 다음으로 보류(방통대 학생은 전부 한국 사용자라 리전을 서울로 옮기면 지연시간 이득이 있으나 Supabase는 프로젝트 리전 변경 기능이 없어 새 프로젝트 생성+데이터 마이그레이션이 필요한 별도 큰 작업) | 완료 (main 병합·배포까지) | - | Supabase Nano→Micro "Free Upgrade" 여부는 사용자가 대시보드에서 직접 확인 후 알려주기로 함(미확인) — 확인되면 컴퓨트 업그레이드 필요 여부 같이 판단. 여유 있으면 DB 리전을 ap-southeast-1→ap-northeast-2(서울)로 옮기는 게 오픈 전 현실적인지도 검토 가능(사용자 요청 시) | 2026-08-24 |
| `claude/statistics-dashboard-design-h2z1mc` (main에 병합·푸시 완료) | 관리자 통계 대시보드(`/admin/statistics`) 신규 구현. 로그인 기능이 아직 없어(모든 시험 응시는 익명, `/exams/**`는 permitAll) 응시 데이터가 DB에 전혀 안 남던 상태였음을 먼저 확인 → **익명 응시 로그 테이블 `exam_attempt_log` 신설**(마이그레이션 `V9`, `user_id`는 나중에 로그인 붙을 때 채울 자리로 nullable 유지). `ExamSolveController.submit()`에서 채점 직후 과목/시험/점수/소요시간을 로그로 남기되, 로그 저장 실패가 실제 채점·제출 흐름을 막지 않도록 try-catch로 격리. `StatisticsMapper`/`StatisticsService`로 오늘·7일·누적 응시 수, 최근 14일 응시 추이, 인기 과목/시험 TOP 10(과목은 평균 정답률도 함께), 콘텐츠 현황(학과·과목·시험·문제 수, 문제 없는 시험 목록)을 집계해 `/admin/statistics` 화면에 노출(사용자 요청으로 콘텐츠 현황 카드를 최상단으로 배치). 차트는 Chart.js(jsdelivr CDN, 기존 CSP `script-src`에 이미 허용되어 있어 CSP 수정 불필요)로 그리되, **CSP에 `'unsafe-inline'`이 없어 인라인 스크립트가 전부 막히는 구조**라 차트 데이터는 JSON이 아니라 `data-*` 속성(CSV 문자열)으로 넘기고 별도 파일(`/js/admin-statistics.js`)에서 읽어 그리는 방식 채택. 기존 디자인 토큰(`--kcbt-accent` 등)과 `.stat-badge` 등 기존 클래스를 재사용해 스타일 통일. **작업 중 발견한 기존 버그도 같이 수정함**: 세션 쿠키가 전혀 없는 완전히 새 방문자가 큰 페이지인 `/exams/{id}/solve`에 바로 접속하면, Spring Security 6의 지연 로딩 CSRF 토큰이 응답이 이미 커밋된 뒤에야 세션을 만들려다 `IllegalStateException: Cannot create a session after the response has been committed`로 500이 나던 것을 재현·확인 → `CsrfFilter` 직후에 붙는 `CsrfTokenEagerLoadingFilter`를 추가해 `/exams/{id}/solve` GET 요청에 한해 응답 작성 전에 토큰을 미리 읽어 세션을 선점하도록 수정(크롤러 등 다른 비로그인 요청까지 매번 세션이 생겨 예전 메모리 초과 이슈가 재발하는 걸 막기 위해 이 경로에만 좁혀서 적용, 홈/시험목록/공지 등 다른 페이지는 여전히 세션 안 생기는 것 확인). 로컬 Postgres(`pg_ctlcluster`) + `bootRun`으로 검증: 관리자 로그인 → 학과/과목/시험/문제 시드 → 실제 `/exams/{id}/solve` curl 제출로 로그 적재·통계 반영 확인, **완전히 새 쿠키 jar로 `/exams/{id}/solve` GET→POST 풀 라운드트립을 재현해 CSRF 버그가 실제로 고쳐졌음을 확인**(200 응답 + JSESSIONID 발급 + 정상 채점·로그 적재), 헤드리스 크롬 스크린샷으로 레이아웃도 확인(단 샌드박스 아웃바운드 네트워크 제약으로 Chart.js CDN 자체 로드는 스크린샷에서 검증 못함 — CSP는 이미 허용돼 있어 실제 배포 환경에서는 정상 로드될 것으로 판단). 사용자 승인 받아 **main에 병합·푸시 완료**(병합 커밋 `cc194e4`). **(추가 수정) 좌우 여백 버그 발견·수정**: 사용자가 "화면이 꽉 차 보인다"고 지적 — `layout:fragment="content"` div에 `class="stats-dashboard"`만 지정했더니 Thymeleaf Layout Dialect가 fragment를 속성 병합이 아니라 통째로 교체하는 방식이라 layout.html이 주는 `class="container mt-4 mb-5 flex-grow-1"`이 통째로 날아가 있었음(다른 관리자 화면과 다르게 뷰포트 전체 폭으로 렌더링). `container` 클래스를 되살려 다른 화면과 동일한 반응형 비율 여백으로 수정, 로컬 스크린샷으로 확인 후 main에 병합·푸시 완료(병합 커밋 `beb7656`) | 완료 (main 병합까지) | `docs/design/statistics-dashboard.md` | 배포 후 실제 www.knou-cbt.com에서 Chart.js 정상 로드, 익명 첫 방문 시험풀기 정상 동작, 통계 화면 여백 확인 권장. **로그인(회원가입) 도입 시 통계 확장(Phase 2: 사용자별 이력, DAU/MAU, 가입자 추이 등)은 위 설계 문서에 미리 정리해둠 — 그 작업 시작할 때 이어서 참고** | 2026-08-24 |
| `claude/adsense-policy-violation-0ufpzb` (main에 병합·푸시 완료) | **애드센스 재심사에서 "가치가 별로 없는 콘텐츠"(최소 콘텐츠 요건 미충족) 정책 위반 통보 받음** — 8/18 세션(`claude/google-adsense-approval-docs-95p157`)에서 이미 승인됐던 건과 별개로, 사용자가 이번에 다시 사이트 검토를 요청했다가 반려된 건. 코드 조사 결과 사이트 내비게이션이 사실상 시험보기(기출문제 풀기 도구)와 공지사항 게시판뿐이었고, 서비스를 설명하는 고유 텍스트 콘텐츠(소개/FAQ 등)가 전혀 없어 구글이 "기능성 도구+얇은 목록"으로 판단했을 가능성이 높다고 보고 대응함. **(1) `/about`(서비스 소개) 신규 페이지**: 무엇을 제공하는지, 방통대 비공식 서비스임과 저작권 안내, 운영자(1인 개발) 소개, 문의처. **(2) `/faq`(자주 묻는 질문) 신규 페이지**: 공식 여부/회원가입 불필요/광고를 붙인 이유/오탈자·정답 오류 제보/학과·과목 추가 요청/모바일 지원/개인정보/문의처 등 8개 항목을 부트스트랩 아코디언으로 구성. **(3) 홈 화면에도 한 줄짜리 카피만 있던 걸 보강**해 서비스 소개 문단(왜 만들었는지, 무엇이 다른지) 추가. (4) 상단 내비게이션·푸터에 두 페이지 링크 추가, `sitemap.xml`에 등록, `SecurityConfig` permitAll에 `/about`,`/faq` 추가. 로컬 Postgres(`pg_ctlcluster`)+`bootRun`으로 두 페이지 200 응답, FAQ 아코디언 8개 항목 정상 렌더링, 홈/내비게이션/푸터 링크 동작을 curl로 확인 후 커밋·푸시. 사용자 확인 받아 **main에 병합·푸시 완료**(병합 커밋 `1af7d66`, PR 없이 직접 병합) — Render 자동배포 트리거됨 | 완료 (main 병합·배포까지) | - | 배포 반영 후 사용자가 애드센스에 재검토 요청 필요. 재검토도 반려되면 문제 문항 자체에 대한 저작권/콘텐츠 오리지널리티 이슈일 가능성도 열어두고 추가 조사 필요(예: 기출문제 원문을 그대로 옮기는 방식 자체가 "다른 출처에서 복사한 콘텐츠"로 보일 수 있음) | 2026-08-27 |

| `claude/app-login-implementation-order-69ml8a` (main에 병합·배포 완료) | **구글/카카오/네이버 소셜 로그인 + 회원 응시 기록(마이페이지) 구현 및 실제 배포·검증까지 완료**. 설계 결정은 `docs/design/oauth-login.md` 참고. 구현 개요는 이전 기록과 동일(`member` 테이블 분리, `provider+provider_id` 식별, 관리자/회원 로그인 진입점 분리, `exam_attempt_log.user_id` 연결, 탈퇴 시 비식별화, `MEMBER_LOGIN_ENABLED` feature flag). **main 병합 후 실제 배포해서 사용자가 직접 3사 로그인 테스트하며 아래 버그들을 실시간으로 발견·수정함**(전부 main에 바로 반영·배포 완료): (1) Render가 프록시로 붙어있는데 `server.forward-headers-strategy`가 꺼져있어 OAuth2 `redirect_uri`가 내부 주소로 잘못 계산되던 문제 → `forward-headers-strategy: framework` 추가. (2) `www.knou-cbt.com`으로 접속 시 구글 콘솔에 등록 안 한 리디렉션 URI라 `redirect_uri_mismatch` → 콘솔에 www 버전도 등록(사용자가 직접). (3) 카카오 KOE205(설정 안 한 동의항목 `account_email` 요청) → 콘솔에서 이메일 동의항목 활성화(선택 동의, 사용자가 직접). (4) 네이버가 "이름(name)"과 "닉네임(nickname)"을 별개 필드로 취급하는데 코드가 `name`을 읽고 있어서 마이페이지에 항상 "네이버회원"으로만 표시되던 버그 → `OAuthAttributes.ofNaver()`가 `nickname` 필드를 읽도록 수정 + scope도 `name`→`nickname`으로 변경(카카오/구글은 애초에 이런 이중 구조가 없어 같은 버그 없음, 코드 재확인함). (5) 컨테이너/DB 세션이 기본 UTC라 `exam_attempt_log.submitted_at` 등 `DEFAULT CURRENT_TIMESTAMP` 값과 관리자 통계의 `LocalDate.now()` 기준이 전부 UTC로 어긋나던 문제(마이페이지 이력에 실제 시각보다 9시간 뒤로 표시되어 발견) → `Dockerfile`에 `ENV TZ=Asia/Seoul` + Hikari `connection-init-sql: SET TIME ZONE 'Asia/Seoul'` 추가, 로컬에서 실제 시각과 비교 검증 완료. **사용자 피드백으로 UX 개선 5건도 같이 반영**: 소셜 로그인 버튼 밑줄 제거, 마이페이지 응시기록 없을 때 "시험보러가기" CTA, 과거 응시 "답안보기"(실제 고른 답 그대로 재현 — 이를 위해 `exam_attempt_log`에 `answers` 컬럼 신설, V11)/"다시풀기" 버튼, 시험 결과 화면에 로그인 시 "시험 기록보기" 버튼, 마이페이지에서 들어온 답안보기 화면은 "목록으로"가 마이페이지로 돌아가도록(방금 푼 직후 경로는 기존처럼 `/exams`로, 경로 출처 기준으로 분기). **현재 상태**: 구글·카카오·네이버 3사 모두 실제 로그인 성공 확인(닉네임 정상 수신 포함). 네이버는 정책상 별도 "네이버 로그인 검수"가 필요해 필요한 캡처(제공정보 활용처, 로그인 절차 단계별) 준비해 **검수 신청 완료, 결과 대기 중**. `MEMBER_LOGIN_ENABLED`는 아직 켜지 않아 네비게이션엔 버튼 비노출 상태(계획대로 조용히 배포 후 검증 단계) | 배포·검증 완료, 네이버 검수 결과 대기 | `docs/design/oauth-login.md` | 네이버 검수 승인되면 `MEMBER_LOGIN_ENABLED=true`로 Render 환경변수 설정해 공개. **공개 시 FAQ/서비스소개/개인정보처리방침/메인화면/공지사항(기존 글 2건 수정 + 신규 1건) 콘텐츠도 같이 갱신 필요 — 초안 전부 `docs/drafts/login-launch-content-updates.md`에 완성해뒀음, 사용자가 "로그인 기능 넣을 때 수정해줘" 요청하면 그 문서 그대로 일괄 반영(코드 4건은 직접 수정, 공지사항 3건은 복붙용 문구로 정리해서 전달)**. 이번 범위에서 의도적으로 미룬 것: 계정 연동(여러 소셜 가입 병합), 관리자용 회원별 응시 이력 상세 조회, 애플 로그인, DAU/MAU 등 로그인 이벤트 기반 통계(`docs/design/statistics-dashboard.md` Phase 2와 연결 — `member.created_at` 채워지기 시작했으니 가입자 추이 등은 바로 이어서 가능) | 2026-09-03 |

<!-- 새 세션에서 작업을 시작하면 위 표에 행을 추가하세요. 상태 예시: 설계 중 / 구현 중 / 리뷰 대기 / 완료 -->
