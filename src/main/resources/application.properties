spring.application.name=cbt

# 데이터베이스 설정
spring.datasource.url=jdbc:log4jdbc:mysql://localhost:3306/KNOU_CBT?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=blossom913@
spring.datasource.driver-class-name=net.sf.log4jdbc.sql.jdbcapi.DriverSpy

# Mybatis 설정
mybatis.mapper-locations=classpath:/mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

spring.thymeleaf.cache=false

# 첨부 파일 크기 제한
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# 첨부파일 저장 경로
file.notice-dir=${user.dir}/uploads/notice
file.exam-dir=${user.dir}/uploads/questions