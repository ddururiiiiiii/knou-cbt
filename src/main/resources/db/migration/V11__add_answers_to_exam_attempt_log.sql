-- 마이페이지에서 과거 응시의 "답안보기"를 그대로 재현하려면 문제별로 실제 고른 답이 필요한데,
-- 지금까지는 점수/총문항수만 남기고 있었음. 문항 순서대로 콤마로 이어붙인 답(건너뛴 문항은 빈 문자열)을
-- 추가로 저장한다. 기존 행은 NULL로 남고(과거 데이터는 재현 불가), 이후 응시부터 채워짐.
ALTER TABLE exam_attempt_log ADD COLUMN answers TEXT;
